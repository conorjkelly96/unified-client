import DeleteIcon from "@mui/icons-material/Delete";

export const ViewForumPostPage = () => {
  return (
    <>
      <h1>ViewForumPostPage</h1>

      {/* TODO: render this button on the page where a student is viewing their own posts */}
      {/* {user.username === username && (
              <Button
                id={id}
                variant="contained"
                size="small"
                endIcon={<DeleteIcon />}
                color="error"
                sx={{ marginLeft: "8px" }}
                //   onClick= {onDelete}
              >
                Delete
              </Button>
            )} */}
    </>
  );
};
